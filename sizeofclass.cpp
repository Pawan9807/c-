#include<iostream>

using namespace std;
 

int main()
{
    landis l;
     //here 'l' is object of class "landis" 
     //but it take size of object class 
     // initial its value is garbage but after putting it get value

    //cout<< "size :" << sizeof(l) <<endl;
    cout<< "size :" << sizeof(l) << endl;

    return 0;
}
